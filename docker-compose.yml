version: "2.18.1"

services:
  ruby_dev:
    build: .
    command: bundle exec rails s -p 3000 -b '0.0.0.0'
    container_name: ruby_container
    ports:
      - "3000:3000"
    volumes:
      - ./:/home/app